services:
  - type: static_site
    name: gloomdev-website
    buildCommand: npm run render-build
    publishPath: dist
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_URL
        value: https://gloomdev-api.onrender.com
